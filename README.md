# note-app

A simple notes application repository.

## Project Structure

- `note-app-client/` — client application directory.

## Getting Started

This repository currently contains the project scaffold. Add your app code inside `note-app-client/`.
